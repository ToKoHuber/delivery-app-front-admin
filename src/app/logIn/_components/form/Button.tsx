import { Button } from "@/components/ui/button";

export default function FormButton() {
  return (
    <div>
      <Button className="w-[416px] px-8 opacity-20 text-[#FAFAFA]">
        Let's Go
      </Button>
    </div>
  );
}
